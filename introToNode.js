// Node.js is the environment used to run JavaScript outside the browser.
// It is built on Chrome's V8 JS Engine.

// Every file in Node.js is treated as a module by default.
console.log(__dirname); // Prints the directory name of the current module.

// simple example of setInterval printing "hello world" every second.
// setInterval(() => {
//     console.log("hello world");
// }, 1000);

const me = 'saad iqbal attar'; // Declares a variable 'me' with the value 'saad iqbal attar'.

// console.log(module); // Prints information about the current module.

// Exports the 'me' variable to make it accessible in other modules.
module.exports = { me };


// pathModule
